#!/usr/bin/env bash

set -e

#shellcheck source=/dev/null
source "$(dirname "$0")"/../service-env.sh

args=()
if [[ -n $RPC_URL ]]; then
  args+=(--url "$RPC_URL")
fi
args+=(catchup ~/validator-identity.json http://127.0.0.1:8899)

set -x
exec solana ${args[@]}
